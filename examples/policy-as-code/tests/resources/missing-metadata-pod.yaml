apiVersion: v1
kind: Pod
metadata:
  name: missing-metadata-pod
  namespace: platform-example
  labels:
    platform.example.com/owner: platform-team
    platform.example.com/service: platform-service
spec:
  containers:
    - name: app
      image: registry.example.internal/platform-service:example
      readinessProbe:
        httpGet:
          path: /healthz
          port: 8080
        periodSeconds: 10
      livenessProbe:
        httpGet:
          path: /healthz
          port: 8080
        periodSeconds: 20
      resources:
        requests:
          cpu: 100m
          memory: 128Mi
      securityContext:
        privileged: false
